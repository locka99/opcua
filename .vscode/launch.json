{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "gdb",
            "request": "launch",
            "name": "Debug - sample-server",
            "target": "${workspaceRoot}/target/debug/opcua-sample-server.exe",
            "cwd": "${workspaceRoot}"
        }
    ],
    "windows": {
        "miDebuggerPath": "C:\\msys64\\mingw64\\bin\\gdb.exe"
    }
}